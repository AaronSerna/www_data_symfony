{% extends 'base.html.twig' %}

{% block title %}Restaurar contraseña{% endblock %}

{% block body %}
    <h1>Restaurar contraseña</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Restaurar contraseña</button>
    {{ form_end(resetForm) }}
{% endblock %}
